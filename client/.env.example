# Development (uses Vite proxy by default — no need to set this locally)
# VITE_API_URL=http://localhost:5000/api

# Production: set this to your deployed API URL
# VITE_API_URL=https://api.omnideal.com/api
